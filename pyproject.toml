[project]
name = "quarto-gradio-text-tool"
version = "0.1.0"
description = "Quarto site embedding a persistent Hugging Face Gradio text tool."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio>=5.0",
]

[dependency-groups]
dev = []
